# Build output
build/
DerivedData/

# CMake / Ninja output from accidental in-tree builds
CMakeCache.txt
CMakeFiles/
CTestTestfile.cmake
cmake_install.cmake
compile_commands.json
Testing/
_deps/
.ninja_deps
.ninja_log
build.ninja

# Compiler and tool caches
.cache/
.ccache/
.qmlcache/
.pytest_cache/
.mypy_cache/
.ruff_cache/
__pycache__/
*.py[cod]

# Temporary editor and operating-system files
*.tmp
*.temp
*.swp
*.swo
*~
.#*

# Runtime diagnostics and crash output
*.log
*.log.*
*.trace
core
core.*

# Arch makepkg working directories and package artifacts
Linux/packaging/arch/src/
Linux/packaging/arch/pkg/
Linux/packaging/arch/opendisplay/
Linux/packaging/arch/*.pkg.tar.*
Linux/packaging/arch/*.src.tar.*

# Generated by xcodegen — run `xcodegen generate`
OpenSidecar.xcodeproj/
Mac/Info.plist
iOS/Info.plist

# Xcode user state
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# Internal working notes (kept locally, not part of the open-source repo)
PROJECT_BRIEF.md
CLAUDE.md
ICON_WORKFLOW.md

# Icon source artwork (kept locally; generated assets are committed — see ICON_WORKFLOW.md)
newicon.svg
newnewicon.png

# Local developer config (signing team etc.)
.env
.claude/

# Fastlane generated output
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/
fastlane/test_output/

# Node / Vite landing-page tooling. The site is built in CI (see pages.yml) and
# deployed straight to GitHub Pages, so the build output is not committed.
node_modules/
dist-ssr/
docs/
